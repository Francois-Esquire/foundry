/**
 * Global test setup — intentionally empty. Earlier revisions installed a
 * silent Effect logger layer; the v1 rewrite no longer calls Effect.log*
 * inside src, so no setup is required.
 */
export {};
