{
  "name": "tui",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "clean": "rm -rf dist .cache .turbo coverage",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opentui/core": "^0.5.11"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
